import * as vscode from "vscode";
import * as client from "vscode-languageclient";

export function registerAll(_context: vscode.ExtensionContext, _languageClient: client.LanguageClient): void {
  return;
}
